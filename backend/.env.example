# Copiá este archivo a .env y completá con tus valores locales.
# Nunca subas .env a GitHub.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=crm_ituarte

# Clave larga y aleatoria en producción (ej. openssl rand -hex 32)
JWT_SECRET=cambiar-por-un-secreto-largo-en-produccion

PORT=3000
